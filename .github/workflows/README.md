# workflows

Definiciones de GitHub Actions que se ejecutan automáticamente en cada push o pull request.

## Archivos

| Archivo | Descripción |
|---|---|
| `ci.yml` | Pipeline de integración continua: instala dependencias, ejecuta lint, build y tests |
