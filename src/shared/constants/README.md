# shared/constants

Constantes globales del sistema.

## Archivos previstos

| Archivo | Descripción |
|---|---|
| `roles.ts` | Roles de usuario (`ADMIN`, `OPERATOR`, `VIEWER`, etc.) |
| `permissions.ts` | Permisos granulares por recurso (`inventory:read`, `sales:write`, etc.) |
