# modules/audit/domain

Capa de dominio del módulo de auditoría.

## Responsabilidad

- Entidades: `AuditLog`, `AuditAction`.
- Reglas de negocio puras sin dependencias de framework ni base de datos.
- Interfaces de repositorio que define el dominio.
